name: Deploy to Server

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install Dependencies
        run: npm ci

      - name: Validate Build
        run: npm run build

      - name: Prepare Server Directory
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            mkdir -p /var/www/LoverMemory/app
            if [ ! -f /var/www/LoverMemory/app/.env ]; then
              echo "Missing /var/www/LoverMemory/app/.env on server"
              exit 1
            fi

      - name: Sync Production Source
        uses: easingthemes/ssh-deploy@main
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          ARGS: "-avzr --delete --exclude='.git/' --exclude='.github/' --exclude='.vscode/' --exclude='node_modules/' --exclude='**/node_modules/' --exclude='dist/' --exclude='apps/*/dist/' --exclude='packages/*/dist/' --exclude='coverage/' --exclude='output/' --exclude='*.log' --exclude='.env'"
          SOURCE: "./"
          REMOTE_HOST: ${{ secrets.SERVER_IP }}
          REMOTE_USER: ${{ secrets.SERVER_USER }}
<<<<<<< HEAD
          TARGET: "/var/www/LoverMemory/app"

      - name: Deploy Services
=======
          TARGET: "/var/www/LoverMemory/dist"

      - name: Refresh Running Web Container
>>>>>>> 383ce75c1a07d96887771b7f962a2c6026c3e277
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
<<<<<<< HEAD
            cd /var/www/LoverMemory/app
            sh scripts/deploy-prod.sh
=======
            WEB_CONTAINER=$(docker ps --filter "label=com.docker.compose.service=web" --format '{{.Names}}' | head -n 1)
            if [ -z "$WEB_CONTAINER" ]; then
              echo "web container not found"
              exit 1
            fi
            docker exec "$WEB_CONTAINER" sh -lc 'rm -rf /usr/share/nginx/html/assets /usr/share/nginx/html/pictures /usr/share/nginx/html/favicon.svg /usr/share/nginx/html/icons.svg /usr/share/nginx/html/index.html /usr/share/nginx/html/manifest.webmanifest /usr/share/nginx/html/sw.js /usr/share/nginx/html/workbox-*.js'
            docker cp /var/www/LoverMemory/dist/. "$WEB_CONTAINER":/usr/share/nginx/html/
            docker exec "$WEB_CONTAINER" sh -lc 'stat /usr/share/nginx/html/index.html && sed -n "1,12p" /usr/share/nginx/html/index.html'
>>>>>>> 383ce75c1a07d96887771b7f962a2c6026c3e277
